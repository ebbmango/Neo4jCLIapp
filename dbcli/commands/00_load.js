const command = {
    command: "load",
    describe: "Creates the database and loads it with the data provided by the CSV file passed as its argument.",
    handler: (argv) => {
      console.log("Task 00 (load) - Work in progress");
    },
  };
  
  module.exports = command;
  